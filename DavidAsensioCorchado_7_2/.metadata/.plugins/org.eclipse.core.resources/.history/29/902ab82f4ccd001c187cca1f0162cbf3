package Ejercicio1;

import java.sql.Connection;
import java.sql.ResultSet;
import java.util.ArrayList;
import java.util.List;

public class gestion {

	public static void main(String[] args) {
		// podriamos acceder a los metodos de manera static
		// en ese caso seria una relacion de uso en UML
		// Elegimos crear un objeto en este caso porque esa
		// clase mantendrá la info de conexion en un atributo
		// private Connection conexion;
		// En este caso es composición en UML porque esta clase
		// cliente contiene una instacia de DBConnectionRefactorizado

		Empleado emp1 = new Empleado("13467954B", "Juan", "Arias", 1350, 2);
		Empleado emp2 = new Empleado("26485914J", "Paco", "White", 1502, 3);
		empresa dbc = new empresa();
		dbc.conectarBD();
		ArrayList<Empleado> listaEmpresa = new ArrayList<>();
		listaEmpresa.add(null);
		System.out.println(dbc.getListaEmpleado(listaEmpresa));

		dbc.buscar("12345678A");

	}

}
